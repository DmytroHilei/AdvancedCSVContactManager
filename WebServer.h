#ifndef WEBSERVER_H
#define WEBSERVER_H
// Add this line so mg_connection is known

void StartWebServer(void);   // No reason for trailing semicolon after declaration
// remove the static handler declaration entirely

#endif // WEBSERVER_H

